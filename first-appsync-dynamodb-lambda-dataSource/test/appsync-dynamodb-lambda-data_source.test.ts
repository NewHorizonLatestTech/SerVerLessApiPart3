// import * as cdk from 'aws-cdk-lib';
// import { Template } from 'aws-cdk-lib/assertions';
// import * as FirstAppsyncDynamodbLambdaDataSourceStack from '../lib/first-appsync-dynamodb-lambda-data_source-stack';

// example test. To run these tests, uncomment this file along with the
// example resource in lib/appsync-dynamodb-lambda-data_source-stack.ts
test('SQS Queue Created', () => {
//   const app = new cdk.App();
//     // WHEN
//   const stack = new FirstAppsyncDynamodbLambdaDataSourceStack.FirstAppsyncDynamodbLambdaDataSourceStack(app, 'MyTestStack');
//     // THEN
//   const template = Template.fromStack(stack);

//   template.hasResourceProperties('AWS::SQS::Queue', {
//     VisibilityTimeout: 300
//   });
});
